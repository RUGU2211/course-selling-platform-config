spring:
  application:
    name: enrollmentservice
  cloud:
    config:
      uri: http://course-selling-platf-config-server-1:8888
  profiles:
    active: default

eureka:
  client:
    service-url:
      defaultZone: http://course-selling-platf-eureka-server-1:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
