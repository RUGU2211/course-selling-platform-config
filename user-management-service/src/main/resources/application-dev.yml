server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://course-selling-platf-mysql-1:3307/users_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.springframework: INFO
    com.courseplatform: DEBUG

jwt:
  secret: dev_secret_change_me
  expiration: 86400000
