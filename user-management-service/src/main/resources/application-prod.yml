server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://course-selling-platf-mysql-1:3307/users_db?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    org.springframework: WARN
    com.courseplatform: INFO

jwt:
  secret: ${APP_JWT_SECRET}
  expiration: 86400000
