spring:
  application:
    name: payment-service
  cloud:
    config:
      uri: http://course-platform-config:8888

  profiles:
    active: default

eureka:
  client:
    service-url:
      defaultZone: http://course-platform-eureka:8761/eureka/  # updated to correct container name
    register-with-eureka: true
    fetch-registry: true
