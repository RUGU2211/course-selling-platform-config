# Config Server - Native profile configuration
# This file is loaded when 'native' profile is active
# Native profile reads from classpath instead of Git
spring.cloud.config.server.native.search-locations=classpath:/config

